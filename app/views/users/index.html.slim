.container-fluid
  h1.title
    | メンバー一覧
  .row.justify-content-center
    = link_to '新規メンバー登録', new_user_path, class:'btn btn-primary btn-lg col-md-4'
  .row
    .col
      table.table.table-striped.table-hover
        thead
          tr
            th.text-center[scope="col"]
              | メンバー名
            th.text-center[scope="col"]
              | 所属グループ
        tbody
          - @users.each do |user|
            tr

            td.text-center
              =user.name
            - if user.group
              td.text-center
                =user.group.name
            - else
              td.text-center
                | 所属なし
