<div class='container-fluid'>
  <h1 class='title'>割合を選択</h1>

  <div class='row'>
    <% @groups.each do |group| %>
      <div class='row justify-content-center mt-4'>
        <%= group.name %>
      </div>
    <% end %>
  </div>
</div>